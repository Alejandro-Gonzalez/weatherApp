import { typography } from './typography';
import { overrides } from './overrides';
import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({ typography, overrides });
